version: 0.0
os: linux
files:
  - source: /
    destination: /home/rudder-docker
  
permissions:
  - object: /
    pattern: "**"
    owner: root
    group: root
  
hooks:
  ApplicationStart:
    - location: deploy.sh
      timeout: 100
      runas: root
